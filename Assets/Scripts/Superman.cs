using System.Collections;
using System.Collections.Generic;
using UnityEngine;


[RequireComponent(typeof(Rigidbody))]
public class Superman : MonoBehaviour
{
    [SerializeField] private damageForce;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
